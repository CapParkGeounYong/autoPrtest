name: Auto Pull Request

on:
  push:
    branches:
      - "feature*"  # Support wildcard matching

jobs:
  pull-request:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Pull Request
        uses: diillson/auto-pull-request@latest
        with:
          source_branch: ${{ github.ref }}              # Use the triggered branch as the source
          destination_branch: "master"                  # If blank, default: master
          pr_title: "Pulling ${{ github.ref }} into master"  # Title of the pull request
          pr_body: |
            :crown: *An automated PR*                   # Body of the pull request
          github_token: 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
        env:
          GH_TOKEN: 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